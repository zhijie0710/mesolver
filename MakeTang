test.exe: mesolver.o test.o
	g++ test.o mesolver.o -o test.exe -larmadillo

test.o: test.cpp
	g++ -c -std=c++11 test.cpp -o test.o -larmadillo

mesolver.o: mesolver.cpp mesolver.h
	g++ -c -std=c++11 mesolver.cpp -o mesolver.o -larmadillo

clean:
	rm *.o *.exe

run: test.exe
	./test.exe
